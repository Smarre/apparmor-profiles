# Profile for sensors-detect.

#include <tunables/global>

/usr/sbin/sensors-detect flags=(complain) {
  #include <abstractions/base>
  #include <abstractions/perl>

  /usr/bin/perl ix,
  /usr/sbin/sensors-detect r,

}

# Profile for uname

#include <tunables/global>

# uname should be inherited; this profile is only a fallaback option.

/usr/bin/uname flags=(complain) {
  #include <abstractions/base>
  #include <abstractions/consoles>
  #include <abstractions/user-tmp>

  /usr/bin/uname mr,
}

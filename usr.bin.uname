# Profile for uname

#include <tunables/global>

# uname should be inherited; this profile is only a fallback option.

/usr/bin/uname {
  #include <abstractions/base>
  #include <abstractions/consoles>
  #include <abstractions/user-tmp>

  /usr/bin/uname mr,
}

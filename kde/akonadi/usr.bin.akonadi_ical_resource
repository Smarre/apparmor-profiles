# Last Modified: Mon Jan 20 15:02:30 2020
#include <tunables/global>

/usr/bin/akonadi_ical_resource {
  #include <abstractions/akonadi_resource_base>
  #include <abstractions/nameservice>
  #include <abstractions/smar_kde>

  signal receive set=kill peer=/usr/bin/akonadi_control,
  signal receive set=term peer=/usr/bin/akonadi_control,

  /usr/bin/akonadi_ical_resource mr,
  owner @{HOME}/.config/akonadi/* rw,
  owner @{HOME}/.config/akonadi_ical_resource_*.lock w,
  owner @{HOME}/.config/kcminputrc r,
  owner @{HOME}/.local/share/akonadi/file_db_data/** r,
  owner @{HOME}/.local/share/user-places.xbel r,
  owner /run/user/[0-9]*/kcrash_* w,

}
